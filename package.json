{
  "name": "rabbitmq-learning",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint .",
    "format": "prettier --write .",
    "fake-victim": "bun --filter fake-victim",
    "orchestrator": "bun --filter orchestrator",
    "worker": "bun --filter worker",
    "token-scraper": "bun --filter token-scraper",
    "build": "bun --filter './packages/*' build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^19.3.0",
    "@commitlint/config-conventional": "^19.2.2",
    "lint-staged": "^15.2.7"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
